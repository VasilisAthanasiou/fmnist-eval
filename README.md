## Source code is at `app/`
- `/data` includes F-MNIST dataset, trained networks and static folder for flask app.
- `/src` includes all the code required to run the project.
## Documents are at `docs/`